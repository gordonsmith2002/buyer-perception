---
description: Checkpoint of the live brand palette before Colour Palette guideline tokens; how to revert.
alwaysApply: true
---

# Brand palette checkpoint (pre-guidelines)

## Live palette before guideline switch

Saved so we can restore the site look that was live on buyerperception.com prior to adopting the official Colour Palette sheet.

**Status:** Guidelines palette is now active in CSS/Tailwind. This checkpoint remains the revert target.

**Git tag:** `palette-live-pre-guidelines`  
**Commit:** `d454d54` — *Swap the About headshot to the new black-and-white portrait.*

| Token | Pre-guidelines (live) | Guidelines (now active) |
| --- | --- | --- |
| Charcoal | `#383637` | `#252626` |
| Terracotta | `#B1694F` | `#B94E30` |
| Sand | `#DFD1B7` | `#E1D4C0` |
| Olive | `#847E42` | `#847E42` |
| Platinum | `#EFEFEF` | `#EFEFEF` |

Also related one-offs that lived with the old palette:

- CTA hover (old): `#9A5A43` → now `#9A3F26`
- How It Works timeline node ring (old): `#E8DFC9` → now `border-sand`

## Where tokens live

- `tailwind.config.ts` — `charcoal`, `terracotta`, `sand`, `olive`, `platinum`, `brandDark`, `brandLight`, `accent`, `creamCard`
- `app/globals.css` — CSS variables `--charcoal`, `--terracotta`, `--sand`, `--olive`, `--platinum`
- `app/opengraph-image.tsx` — hardcoded hex for OG art
- Visual side-by-side: `/brand-compare` (`app/brand-compare/page.tsx`) — left column is this checkpoint

## How to revert to this live version

```bash
git checkout palette-live-pre-guidelines -- \
  tailwind.config.ts \
  app/globals.css \
  app/opengraph-image.tsx
```

Then restore CTA hover / timeline ring from the pre-guidelines values above if those files have moved on, or check out the tagged versions:

```bash
git checkout palette-live-pre-guidelines -- \
  components/NavBar.tsx \
  components/BookButton.tsx \
  components/HowItWorksSection.tsx
```

Or reset the branch to the tag if a full rollback is needed (only with explicit user approval):

```bash
git switch -c restore-pre-guidelines-palette palette-live-pre-guidelines
```

Do not delete the `palette-live-pre-guidelines` tag or rewrite this checkpoint without asking the user.
